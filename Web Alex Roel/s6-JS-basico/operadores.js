//Operadores aritmeticos

let a = 4;
let b = 5;
a=Number(a);
b=Number(b);
let s = a + b;//Suma
let r = a - b;//Resta
let m = a * b;//Multiplicacion
let d = a / b;//Division
let mo = a % b;//Modulo
let p = a ** b;//Potencia

//Operadores de asignacion

let c = 4; //Asignar datos a una variable
let suma = c + 4; //Asignar una suma de un dato con variable
suma += c; //Suma el valor de suma + 4
suma -= c; //Resta el valor de  suma -4
suma *= c; //Multiplica el valor de suma *2
suma /= c; //Divide el valor de suma 2
suma **= c; //Potencia el valor de suma a la 2
suma %= c; //Modula el valor de suma sobre 2

//Operadores de comparacion
4 == 4; //Si los valores son iguales 
4 === '4'; //Si los varoles son iguales y el tipo de dato
4 != 4; //Si los valores son distintos
4 < 2; //Si el primer valor es menor
4 > 3; //Si el primer valor es mayor
4 <= 2; //Si el primer valor es menor o igual
4 >= 3; //Si el primer valor es mayor o igual

//Operadores logicos
4==4 && 4==4;//Ambas comparaciones son verdaderas regresa true
4==4 || 4==4;//Alguna comparacion es verdadera regresa true
! 4==5; //Vuelve al valor contrario de un valor booleano regresa true

//Operadores de incremento
let a=0;
a++; //Incrementa de 1 en 1 despues de una ejecucion
a--; //Decrementa de 1 en 1 despues de una ejecucion
++a; //Incrementa de 1 en 1 antes de una ejecucion
--a; //Decrementa de 1 en 1 antes de una ejecucion